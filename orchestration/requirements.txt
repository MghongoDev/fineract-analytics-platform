# Extra dependencies layered on top of the apache/airflow base image.
# dbt is installed into an isolated virtualenv (see dbt-requirements.txt)
# because its protobuf version is incompatible with Airflow's telemetry stack.
clickhouse-connect>=1.7.1,<2.0
psycopg[binary]>=3.1,<4
requests>=2.31,<3
prometheus-client>=0.20,<1
apache-airflow-providers-postgres>=5.11
